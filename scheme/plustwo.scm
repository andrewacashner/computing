; plustwo.scm 2017/10/28
; Daddy and Ben

(define plustwo
  (lambda (x)
    "Given a number, return the number plus two."
    (+ 2 x)))

(define double
  (lambda (x)
    "Given a number, return twice the number."
    (+ x x)))

